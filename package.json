{
  "name": "chef-bot-conservative",
  "version": "2.0.0",
  "scripts": {
    "start": "rm -f state.json trade_history.json && npx tsx src/index.ts",
    "dev": "npx tsx watch src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
